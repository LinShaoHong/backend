server.port: 9727

spring:
  application:
    name: XFG-Server
  datasource:
    url: jdbc:mysql://${MYSQL_SERVER:localhost:3306}/xfg?useUnicode=true&characterEncoding=UTF8&useSSL=false&rewriteBatchedStatements=true&autoReconnect=true&failOverReadOnly=false&allowPublicKeyRetrieval=true
    username: ${MYSQL_USER_OF_XFG:root}
    password: ${MYSQL_PASSWORD_OF_XFG:root}

footprint.max: ${FOOTPRINT_MAX:100}
retrieve.password.key: ${RETRIEVE_PASSWORD_KEY:abcd}
