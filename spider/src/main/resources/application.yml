spring:
  application:
    name: Spider-Server
  datasource:
    spider:
      url: "jdbc:mysql://localhost:3306/spider?useUnicode=true&characterEncoding=UTF8&useSSL=false&rewriteBatchedStatements=true&autoReconnect=true&failOverReadOnly=false"
      username: "root"
      password: "root"

server:
  port: 1388
